import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { Nunito } from "next/font/google";



export const metadata: Metadata = {
  title: 'Jude\'s Coastal Kitchen | Fresh Seafood & Sunset Views in Negombo',
  description: 'Family-run coastal kitchen in Negombo. Authentic Sri Lankan seafood, famous sunset views from our second-floor balcony. Open daily.',
  openGraph: {
    "title": "Jude's Coastal Kitchen | Authentic Coastal Dining",
    "description": "Experience fresh Indian Ocean seafood at our family-run kitchen on Negombo Beach Road.",
    "siteName": "Jude's Coastal Kitchen"
  },
};

const nunito = Nunito({
  variable: "--font-nunito",
  subsets: ["latin"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${nunito.variable} antialiased`}>
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
